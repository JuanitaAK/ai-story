import { Loader } from "../components/organismes/Loader";

export const LogInPage = (): JSX.Element => {
  return (
    <div className="block content-center justify-center w-full">
      <Loader />
    </div>
  );
};

export default LogInPage;
