// // import the Navbar component from the Navbar file

// import { render } from "@testing-library/react";
// import Navbar from "../Navbar";

// describe("Navbar", () => {
//   render(<Navbar />);
//   it("should render a navbar with links to stories, new story and account if user is logged in", () => {
//     // render the Navbar component
//     //login the user
//     // check if the links are rendered
//     // check if the links are correct
//   });

//   it("should not render the links  stories, new story and account if user is not logged in", () => {
//     // render the Navbar component
//     // check if the links are not rendered
//   });
// });
